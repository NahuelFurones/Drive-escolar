<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Crea una cuenta</title>
  <link href="/css/styles.css" rel="stylesheet">
</head>
<body class="bg-cover bg-center min-h-screen" style="background-image: url('/src/fondo_chaca.jpg')">
  <div class="flex items-center justify-center min-h-screen bg-black bg-opacity-50">
    <div class="bg-white bg-opacity-10 rounded-lg p-8 w-full max-w-md text-white">
      <img src="/src/chacabuco_logo.png" alt="Logo" class="mx-auto mb-4 w-40 h-24"/>
      <h2 class="text-2xl font-bold mb-4">Crea una cuenta</h2>
      <p class="mb-4">¿Ya estás registrado? <a href="/login" class="underline">Inicia sesión</a>.</p>
      <% if (error) { %>
        <div class="bg-red-500 text-white rounded p-2 mb-2 text-center"><%= error %></div>
      <% } %>
      <form method="POST">

        <div class="mb-4 text-left">
          <label class="block mb-1">CORREO ELECTRONICO</label>
          <input type="email" name="email" class="w-full px-3 py-2 rounded bg-black bg-opacity-60 text-white" required>
        </div>

        <div class="mb-4 text-left">
          <label class="block mb-1">CONTRASEÑA</label>
          <input type="password" name="password" maxlength="64" class="w-full px-3 py-2 rounded bg-black bg-opacity-60 text-white" required>
        </div>

        <div class="mb-4 text-left">
          <label class="block mb-1">TELEFONO MOVIL</label>
          <input type="text" name="telefono" maxlength="10" pattern="[0-9]{10}" class="w-full px-3 py-2 rounded bg-black bg-opacity-60 text-white" required>
        </div>

        <button type="submit" class="w-full py-2 bg-white text-black font-bold rounded hover:bg-gray-200">REGISTRATE</button>
      
      </form>
    </div>
  </div>
</body>
</html>
